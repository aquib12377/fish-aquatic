# Install into a venv created with: python3 -m venv --system-site-packages ~/fishenv
# (--system-site-packages lets the venv see the apt-installed picamera2/libcamera bindings)
adafruit-circuitpython-pca9685
adafruit-circuitpython-motor
adafruit-blinka
gpiozero
RPi.GPIO

# Install separately via apt, NOT pip, on Raspberry Pi OS Bullseye/Bookworm:
#   sudo apt install -y python3-picamera2 ffmpeg --no-install-recommends
# ffmpeg is required by camera_module.py's FfmpegOutput to mux video into .mp4
